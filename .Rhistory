publish(DDP_Slidify, host="dropbox")
library(slidify)
publish(DDP_Slidify, host="dropbox")
publish(DDP_Slidify, host="dropbox")
publish(DDP_Slidify, host="dropbox")
publish(title="Car vs Car", 'index.html, host='rpubs'')
publish(title='Car vs Car', 'index.html', host='rpubs'')
publish(title='Car vs Car', 'index.html', host='rpubs')
publish(title='Car vs Car', 'index.html', host='rpubs')
data(mtcars)
d <- mtcars
d$names <- rownames(mtcars)
library(dplyr)
head(d)
f <- c("Datsun 710", "Valiant")
filter(d, f)
?filter
filter(d, "Valiant")
filter(c, names %in% f)
filter(d, names %in% f)
f <- c("Datsun 710", "Valiant")
filter(mtcars, names %in% f)
library(dplyr)
data(mtcars)
mtcars$names <- rownames(mtcars)
head(mtcars)
f <- c("Datsun 710", "Valiant")
filter(mtcars, names %in% f)
filter(mtcars, names=="Valiant")
library(slidify)
filter(mtcars)
mtcars$names
mtcars$names=="Valaint"
mtcars$names=="Valiant"
filter(mtcars, mtcars$naems=="Valiant")
filter(mtcars, mtcars$names=="Valiant")
publish(title="Car vs Car", 'index.html', host='rpubs')
